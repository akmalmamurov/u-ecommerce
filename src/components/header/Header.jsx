import {Navbar} from "../navbar";
import HeaderMid from "./HeaderMid";

export const Header = () => {
  return (
    <header>
      <HeaderMid />
      <Navbar />
    </header>
  );
};

export default Header;
